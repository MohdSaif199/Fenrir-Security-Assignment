# React + Vite

# This project is created with React + Vite
# Techstack used is React,Tailwind for styling ,Context for theme toggling and BrowserRouter for navigation
# To run the project use npm install
# After the node modules are installed use npm run dev to run the server
